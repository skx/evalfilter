//
// Test `&&` in the `if` statement.
//


//
// Look for `nobody` with a high Uid.
//
if ( ( Username == "nobody" ) && ( Uid > "1000" ) ) {
  print( "**********************************************************************\n");
  return true;
}


//
// All over.
//
return false;
